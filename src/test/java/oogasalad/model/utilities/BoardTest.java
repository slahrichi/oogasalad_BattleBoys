package oogasalad.model.utilities;
  /*
import static org.junit.jupiter.api.Assertions.*;

import java.util.List;
import oogasalad.model.utilities.tiles.Cell;
import oogasalad.model.utilities.tiles.ShipCell;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

class BoardTest {

  private Board myBoard = new Board(new int[][]{{1, 0, 0}});
  Coordinate coord = new Coordinate(1,2);
  Cell cell =  new ShipCell(coord, 1);
  List<Coordinate> relative;

  @Test
  void initialBoardIsEmpty() {
    for (Cell c : myBoard.listPieces()) {
      assertEquals(c, null);
    }
  }

  @Test
  void boardUpdate() {
      myBoard.place(coord, cell);
      assertEquals(myBoard.checkCell(coord), cell);
    }

  @Test
  void piecesUpdate() {
    boardUpdate();
    assertEquals(myBoard.listPieces().get(0), cell);
    assertEquals(myBoard.listCoordinates().get(0), coord);
  }



}

   */